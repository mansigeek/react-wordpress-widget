import type { CardStyleClasses } from "../utils/class-generator";

export const defaultCardStyles: CardStyleClasses = {
    backgroundColor: "white",
    bodyFontSize: "sm",
    borderColor: "gray-200",
    borderRadius: "lg",
    borderWidth: "thin",
    padding: "md",
    shadow: "sm",
    showBorder: true,
    textColor: "gray-900",
    titleFontSize: "lg",
    titleFontWeight: "semibold",
};
